# Project Alpha Requirements
# Install with: pip3 install --user --break-system-packages -r requirements.txt

# Core HTTP library
requests>=2.32.0

# Google Drive API
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.127.0

# Process monitoring for transcriber
psutil>=5.9.0

# OpenAI Whisper (auto-installed by transcriber if missing)
openai-whisper

# Note: FFmpeg must be installed separately
# FFmpeg: brew install ffmpeg
